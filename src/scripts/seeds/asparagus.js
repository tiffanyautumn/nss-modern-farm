//define and export a fn for creating a seed
//fn name createAsparagus

export const createAsparagus = () => {
    const asparagusSeedling = {
        type: "Asparagus",
        height: 24,
        output: 4,
    } //creates a seed object
    return asparagusSeedling
}
